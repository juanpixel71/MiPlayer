name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: |
          npm install

      - name: Prepare Web Assets
        run: |
          mkdir -p www
          cp -r *.html *.js *.json *.png www/ 2>/dev/null || true

      - name: Backup Manifest & Generate Fresh Android Platform
        run: |
          if [ -f "android/app/src/main/AndroidManifest.xml" ]; then
            cp android/app/src/main/AndroidManifest.xml /tmp/AndroidManifest.xml
          fi
          
          rm -rf android
          npx cap add android
          
          if [ -f "/tmp/AndroidManifest.xml" ]; then
            cp /tmp/AndroidManifest.xml android/app/src/main/AndroidManifest.xml
          fi

      - name: Replace Android Icons with Custom icon.png
        run: |
          if [ -f "icon.png" ]; then
            echo "Reemplazando iconos de Android con icon.png..."
            find android/app/src/main/res/ -name "ic_launcher*.png" -exec cp icon.png {} \;
          fi

      - name: Sync Capacitor project
        run: |
          npx cap sync android

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Build Android APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
